package org.budgetanalyzer.transaction.service.dto;

/** Stable service-layer file-level warning codes for preview responses. */
public enum PreviewFileWarningCode {
  /** The current user has already imported the exact same file bytes. */
  FILE_ALREADY_IMPORTED
}
