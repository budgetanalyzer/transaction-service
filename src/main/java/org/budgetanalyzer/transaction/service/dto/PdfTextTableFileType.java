package org.budgetanalyzer.transaction.service.dto;

/** Supported source file types for configurable PDF text-table parsing. */
public enum PdfTextTableFileType {
  /** A text-based PDF with extractable embedded text. */
  TEXT_PDF
}
