AI SESSION HANDLER WORKER PROTOCOL

CONTEXT
- workspace_path: /workspace/transaction-service
- plan_path: /workspace/transaction-service/docs/plans/codebase-simplicity-and-defensive-branch-reduction.md
- state_path: /workspace/transaction-service/.ai-session-handler/codebase-simplicity-and-defensive-branch-reduction.json
- run_id: 20260714T095217Z-phase-8
- transcript_path: /workspace/transaction-service/.ai-session-handler/transcripts/20260714T095217Z-phase-8.txt
- selected_phase_id: phase-8
- selected_phase_title: Simplify Controllers, Mapping, and Documentation

INSTRUCTIONS
- Read repository instructions first, including the nearest AGENTS.md.
- Inspect current repository state before editing.
- Implement exactly selected phase phase-8: Simplify Controllers, Mapping, and Documentation.
- Do not proceed to later phases.
- Follow the selected phase plan unless repository reality contradicts it.
- Run the validation commands listed in the selected phase.
- Do not run git commit, push, checkout, reset, clean, stash, branch creation, or automatic worktree operations.
- Update only files required for the selected phase.
- Treat state_path as runner-owned and read-only. Do not create, edit, replace, or delete the state file; report the phase outcome only through the terminal marker.
- Do not make design-changing guesses. If implementation requires an unplanned product, architecture, schema, API, or workflow decision, stop with <phase-needs-clarification>.
- Treat user clarification as a first-class stop state, not as failure.
- End with exactly one terminal marker from the grammar below.
- Do not emit more than one terminal marker.

FAILURE MODES
- Use <phase-blocked> when the phase cannot be completed without external action.
- Use <phase-needs-clarification> when a specific user decision is required.
- Use <phase-complete> only after implementation and validation for this phase are complete.

PREVIOUS STATE SUMMARY START
schema_version: 1
plan:
  path: /workspace/transaction-service/docs/plans/codebase-simplicity-and-defensive-branch-reduction.md
  sha256: 86a90b0aa39979297b00f46e811598fedca40b6518819b1f2a64d70bbf0dda44
  accepted_at: 2026-07-14T08:48:51Z
completed_phase_ids: phase-1, phase-2, phase-3, phase-4, phase-5, phase-6, phase-7
current_phase:
  id: phase-8
  title: Simplify Controllers, Mapping, and Documentation
stop: none
last_run:
  run_id: 20260714T094651Z-phase-7
  phase_id: phase-7
  status: phase-complete
  started_at: 2026-07-14T09:46:51Z
  finished_at: 2026-07-14T09:52:17Z
  exit_code: 0
  transcript_path: /workspace/transaction-service/.ai-session-handler/transcripts/20260714T094651Z-phase-7.txt
  summary: Implemented phase 7 only. Simplified CSV date parsing to primary plus optional distinct simplified formatter; removed redundant PDF minimum-row recheck and missing-header branch after candidate header filtering; simplified PDF date-format validation to the fixed allowlist; removed pure BusinessException rethrows in registry/PDF text extraction while preserving wizard field-error translation. Added focused extractor tests for CSV date-only fallback, PDF minimum-row candidate filtering, and mapped-header filtering before value lookup. Updated statement import docs for the formatter and PDF candidate-row invariants. Validation passed: ./gradlew clean spotlessApply, ./gradlew clean build, and git diff --check. Full build exercised the CSV/PDF wizard preview, normal saved PDF text-table preview, and static PDF fixture parser tests.
PREVIOUS STATE SUMMARY END

SELECTED PHASE BODY START

### Goal

Remove residual presentation-layer noise, complete documentation in the same work, and verify that
the codebase is measurably simpler without weakened boundaries.

### Scope

- Remove search filter-group helper methods used only to produce boolean log fields; keep concise
  page/sort/request logging without sensitive filter values.
- Remove unenforced Bean Validation annotations from response DTOs while preserving OpenAPI
  required/optional metadata.
- Consolidate duplicate statement-format response mapping into one mapper/factory.
- Remove redundant local JSON inclusion annotations when global serialization config already owns
  the behavior, after response serialization characterization confirms equivalence.
- Correct stale comments such as the PDF parser-type "future" wording and file-import duplicate
  rejection language that no longer matches advisory reupload behavior.
- Re-run a production call-site and null/branch inventory. Review every remaining concentrated file
  and document why high-branch areas are either business policy or external-boundary handling.
- Update `README.md`, `AGENTS.md`, `docs/api/README.md`, `docs/statement-import.md`,
  `docs/duplicate-detection.md`, `docs/saved-views.md`, `docs/domain-model.md`, and
  `docs/database-schema.md` only where the implemented internal contract or documented behavior is
  affected. Do not copy this plan wholesale into user documentation.

### Non-goals

- Do not chase a target line count, null-check count, cyclomatic score, or class size by weakening
  behavior or compressing readable code.
- Do not implement any requirement-level option from the discussion table.
- Do not remove OpenAPI endpoint documentation merely because it contributes controller lines.

### Required context

- Results and documentation changes from Phases 1-7.
- All repository docs named in Scope and `application.yml` serialization settings.
- `../service-common/docs/code-quality-standards.md`.

### Implementation notes

- Lead the final audit with concrete remaining branch rationales: external/untrusted input,
  authorization/ownership, optional API semantics, persistence state, or supported business policy.
- A remaining branch with no reachable state and no actionable caller response should be removed or
  explicitly deferred with evidence.
- Keep logging structured and free of file contents, transaction descriptions, tokens, and other
  sensitive data.
- Documentation changes are part of each preceding phase; this phase reconciles the complete set and
  must not be used to postpone known updates.

### Validation

Run the required full sequence one final time:

```bash
./gradlew clean spotlessApply
./gradlew clean build
```

Then run focused inventories:

```bash
rg -n "Objects\.requireNonNull|== null|!= null" src/main/java
rg -n "canHandle|refreshCsvExtractors|extractEntities|getAllExtractors" src/main src/test
rg -n "ViewTransactionResponse|MembershipType|TransactionDescriptionMatchResult" src/main src/test
```

Review generated OpenAPI/serialized response tests and all documentation links.

### Completion criteria

- All behavior-preserving findings are resolved or retained with a concrete boundary/policy reason.
- No test-only production compatibility API, duplicate parser lifecycle, dynamic extractor cache, or
  repeated saved-view membership implementation remains.
- Authorization, ownership, soft delete, preview-token security, parser fixtures, duplicate policy,
  and HTTP response contracts remain green.
- The full build passes and the nearest affected documentation is current.
- Any approved requirement reduction is still isolated to a separate follow-on plan with explicit
  migration and compatibility work.
SELECTED PHASE BODY END

TERMINAL MARKER GRAMMAR START
<phase-complete>summary</phase-complete>
<phase-blocked>reason</phase-blocked>
<phase-needs-clarification>specific question for user</phase-needs-clarification>
TERMINAL MARKER GRAMMAR END
